# v0.4.19 Regression Matrix — Unified Formula Library

- Status: Release-candidate facts frozen; external release evidence pending
- Date: 2026-08-16
- Amended: 2026-08-30 (revision-4 exact sets, release facts, preview/Julia closure, and fail-closed release blockers)
- Spec: [Unified Formula Library and FRM-like Language Contract v1](../specs/unified-formula-library-v1.md)
- ADR: [ADR 0008](../adr/0008-unified-formula-library-contract.md)

This matrix distinguishes contract acceptance from implementation completion.
Rows remain pending until backed by named, reproducible evidence. Public Level 1
must not contain controlled source, private paths, credentials, reversible
intermediates, or fabricated historical claims.

Commit 31 freezes the final public facts at **534 published Standard
Definitions**, with **94** overlapping Classic formulas and
**21** Guides. Julia activation remains exact-set gated and fail-closed.
These facts do not make the candidate release-ready: physical-device and
screen-reader evidence, the SHA-bound deployed rollback drill, hosted Preview
smoke, and final independent review remain fail-closed gates.

## A. Slice 0 contract and prototype gates

| ID | Gate | Required evidence | Status |
|---|---|---|---|
| A1 | parameter grammar parses real/complex/function declarations, hard real domain, and optional Classic binding | parser prototype + positive/negative fixtures | pass (prototype) |
| A2 | declaration names, system inputs, constants, sections, stdlib names, and Classic slots cannot collide | parser/type negative fixtures | pass (prototype) |
| A3 | canonical source round-trips through parser/formatter; sourceRevision and semanticHash stay stable | hash/formatter prototype tests | partial: all 534 published Definitions have index-matching sourceRevision/semanticHash and preserve semanticHash through formatter round-trip; comments/format only affect sourceRevision; seven normative/manual examples also round-trip, but the v0.4.19 front end still accepts parenthesized nested magnitude that its formatter cannot round-trip; canonical v1 forbids nesting and writer activation remains blocked on parser rejection |
| A4 | stdlib v1 branch cuts, inverse trig, deterministic round, NaN/Infinity, and nonFinite termination are frozen | CPU table fixtures + typed contract tests | pass: original isolated-core evidence retained; production `frm-v1-stdlib`, `frm-v1-classic-guards`, `frm-v1-backend`, and `frm-v1-core-isolated` suites pass; C8 runtime evidence is complete, while device evidence remains C9 and unified app integration remains C1/C10 |
| A5 | `standard32` records evaluation order and tolerance-based CPU/WebGL conformance | orbit/event fixture prototype | pass: original isolated CPU/SwiftShader float-readback, primitive-f32 stdlib, branch/event/continue parity repeated 5×; current C8 evidence adds 534 framework compile/link checks and 1,068 bounded CPU/GPU orbit/event/continue probe pairs; full physical-device/GPU image-difference coverage remains C9/F6 |
| A6 | every published canonical source is <= 65,536 UTF-8 bytes; private legacy projections do not imply publication | private projection + public published-N aggregate | pass: the revision-4 public gate verifies 534/534 content-addressed source bodies and a 927-byte maximum; private projection evidence creates no publication right |
| A7 | 65,536 accepted and 65,537 rejected using UTF-8 bytes | shared boundary prototype | pass (isolated Definition asset boundary; cross-surface drift pending) |
| A8 | legacy 65,537..262,144 source is preserved, read-only, non-executable, non-publishable, and non-overwritable | portable reader/writer fixtures | partial: isolated v3/v2 reader preserves it read-only and legacy/v3 writers remain disabled; publish/API drift gate pending |
| A9 | source-budget constants and error codes are generated consistently across client, portable, compiler/worker, API/publish, and DB | drift matrix | partial: Formula v1 Safety, portable import/write, gated Mine API/service, and forward-only DB migration enforce 65,536 UTF-8 bytes; publish/worker/client generated-constant drift remains pending |
| A10 | Standard UUIDv5 algorithm is deterministic under frozen namespace; Mine UUIDv4 has same shape | identity prototype tests | pass (production-isolated identity domain; Standard generation remains frozen input) |
| A11 | manifest has an exact unique neutral-ID set | generated-manifest test | pass: production-isolated validator accepts the frozen exact set and rejects duplicate, private, or extra fields |
| A12 | typed aliases cover every frozen source, runtime, and Guide alias exactly once | alias bijection/reverse-audit tests | pass: the immutable typed alias index and reverse audit both match their frozen exact sets |
| A13 | resolver consumes aliases only at boundary; compiler/runtime cannot inspect alias kind | dependency and behavior tests | pass (production-isolated revision-pinned resolver seam; app activation pending) |
| A14 | Formula Definition/Profile/Record/Document schemas reject cross-layer ownership drift | schema tests | pass for production-isolated Definition/Profile/Record validators and reader-only Document v3 snapshots |
| A15 | Document v3 and Envelope v2 embed and verify a complete Formula Snapshot | reader implementation + fixtures | pass (production-isolated reader; flat self-contained snapshot and content-addressed assets; writer disabled) |
| A16 | dual readers accept legacy v1/v2 and v3/v2; future, corrupted, tampered, unsupported profile/version open read-only with stable reasons | fixture matrix | pass in isolated production reader; deployed Preview reread remains D8 |
| A17 | Standard and Mine walking skeletons replay offline without catalog/cloud/network | integration prototype | partial: scope isolation/atomic fallback pass; full replay pending |
| A18 | Atlas selector prototype receives compact Record/Profile metadata only; no source/runtime in initial payload | selector/payload tests | pass (prototype) |
| A19 | Explore selector prototype resolves one Formula ID to pinned Definition/Profile without registry-as-catalog behavior | route/resolver tests | partial: revision-pinned resolver/store and published Record one-shot Open/Remix handoff pass with exact Definition/Profile runtime inputs; unified selector and full offline replay remain pending |
| A20 | five public-safe teaching fixture packages have seven-locale key parity and explicit fallback/noindex state | content-schema tests | pass: 5 × 7 fallback/noindex prototype |
| A21 | baseline full unit suite is green before implementation | `npm run test:run` | pass: 113 files / 1,099 tests |
| A22 | build, Atlas/Explore payload, cold/hot select, interaction, and memory baselines are captured before scale-up | baseline report | partial: build/route/Atlas captured; browser/GPU/real-device pending |

**2026-08-20 A3 clarification:** the active reader hashes exact source bytes, so
comments, formatting, line endings, and terminal LF may change `sourceRevision`
without changing `semanticHash`. The gated writer separately requires its
deterministic formatter form before new persistence or publication.

## B. Nine-waiver gates

The public test data contains only public-safe formula aliases, reason categories,
dispositions, and project-authored fixtures. Original source and private evidence
remain Level 2.

| ID | Gate | Required evidence | Status |
|---|---|---|---|
| B1 | exactly nine dispositions: seven released T1 and two released T2 | L2 exact-set join + L1 cardinality/schema verifier | pending |
| B2 | `carr2289`: immutable host input, fresh local state, inverse trig | parser/type + stdlib + CPU/WebGL fixtures | pending |
| B3 | `f'functionike`: malformed source is not permissively accepted; rehabilitated source follows isolated spec | negative parser + lineage + orbit fixtures | pending |
| B4 | `fly`: ordinary control flow/predicate with no ID special case | compile + orbit/event fixtures | pending |
| B5 | `frm-d1`: historical invalid status disclosed; runnable rehabilitation is pinned and may share semanticHash with companion | Record/schema + semanticHash fixtures | pending |
| B6 | `g-3-03-m`: missing original is disclosed; deterministic round and isolated implementation close execution | rights + stdlib + orbit fixtures | pending |
| B7 | `julia`, `mand_1`, and `mandel`: selected-entry canonicalization closes trailing-content cases while canonical trailing tokens remain fatal | scanner/compiler positive + negative fixtures | pending |
| B8 | `mandelbrotbc3`: builtin constants remain immutable; intended local is alpha-renamed generally | parser/type + CPU/WebGL fixtures | pending |
| B9 | no waiver introduces Formula ID, scope, trust, or provenance branches in parser/compiler/runtime/safety | static/dependency audit | pending |
| B10 | all nine have Definition, default Profile, Record, rights lane, sourceRevision, semanticHash, and conformance evidence | L2 completeness + L1 aggregate | pending |

## C. Language, Definition, and runtime gates

| ID | Gate | Required evidence | Status |
|---|---|---|---|
| C1 | one compiler path accepts Standard, Mine, and future Community definitions under identical contracts | compiler/API tests | partial: identical scope-free executable projection and compiler-adapter input pass; app integration pending |
| C2 | Safety Envelope limits are finite, versioned, generated, and never scope/ID dependent | envelope drift tests | partial: isolated asset boundary reuses frozen core source ceiling and omits Formula ID/scope; cross-surface generation pending |
| C3 | source -> typed semantic IR -> execution IR is deterministic | golden serialization/hash fixtures | pass (isolated canonical hash + CPU/GLSL artifact fixtures) |
| C4 | backend optimization never changes sourceRevision/semanticHash | backend equivalence tests | partial: resolver verifies and carries both hashes independently of backend build/artifact revision; optimization equivalence matrix pending |
| C5 | all published Definitions compile and expose declared schema/channels/capabilities | published-N manifest build | pass: exact decision-revision-4 set joins 339 separated-independent rewrites + 106 direct adaptations + 89 project-owned implementations; all 534 source/semantic hashes, versioned parameter descriptors, v1 backend compiles, and full framework source assemblies pass |
| C6 | every published Definition has at least one verified default Profile | published-N manifest gate | pass: the source-bound Record Profile artifact is `ready` for exactly 534/534 published Definitions and is verified before Record master generation |
| C7 | generated runtime is sharded/lazy and not a second source of truth | build manifest + dependency audit | pass: the light 534-row index contains no Definition body; 534 immutable `definitions/<sourceRevision>.frm` bodies are fetched only on selection and rebuilt from pinned revision-4 inputs |
| C8 | CPU/WebGL orbit/event conformance passes declared tolerances for every published implementation | full conformance suite | pass: exact-set report merger covers 534/534 final shaders; 534 complete framework compile/link checks + 1,068 bounded CPU/GPU orbit/event/continue probe pairs pass under the declared diagnostic renderer |
| C9 | image-difference and deterministic preview generation pass representative GPU/device matrix | visual evidence | partial: source-derived 1200 x 750 Record masters cover 534/534 published Definitions, with 43 accepted deviations explicitly policy-bound and deterministic rerenders verified; representative physical-device GPU/image-difference evidence remains F6 |
| C10 | unsupported NumericProfile opens read-only; Open Compatible Copy is explicit and preserves original | reader/UI/integration tests | pending |

## D. Document, portable, cloud, and lifecycle gates

| ID | Gate | Required evidence | Status |
|---|---|---|---|
| D1 | v3 work embeds exact source/schema/resolved params/profile and final artwork state | schema + round-trip tests | pass for isolated gated writer/readback; application writer activation remains off |
| D2 | `.frm`, `.fractal-formula.json`, and `.fractal.json` have deterministic import/export and tamper checks | file fixtures | partial: isolated deterministic writer/import, typed tamper rejection, multi-entry selection, and artwork identity preservation pass; UI/file-picker integration remains pending |
| D3 | Standard and anonymous Mine Remix create editable copies with lineage; no originalSource leakage | integration + leakage tests | pass for isolated new-UUID fork/import and minimal revision-pinned lineage; OTP/UI integration pending |
| D4 | My Formulas preserves invalid draft, last-valid head, save-time validation, rename/delete, quota, optimistic concurrency, auth recovery, and owner isolation | API/DB/UI tests | partial: validated dual heads, explicit rehabilitation, owner/idempotency/quota/account-deletion RPC seam and default-off route land; rename/delete/auth-recovery UI and hosted DB behavior pending |
| D5 | cloud asset/revision schema uses owner isolation and the same source/safety budget | migration review + local/hosted tests | partial: forward-only private append-only revision schema, composite head ownership, runnable-head guard, app rehash/Safety/Profile validation, and 65,536-byte DB gate land; migration not applied to hosted environments |
| D6 | public publication cannot expose controlled original source or private metadata | API/route leakage tests | pending |
| D7 | backup/restore rehearsal succeeds before Production migration authorization | runbook evidence | pending |
| D8 | deployed dual reader rereads old/new documents before writer gate | Preview smoke | pending |
| D9 | writer feature flag defaults off and can be disabled without losing read support | deployment smoke | partial: portable writers require explicit capability and Mine route env gate defaults off while v1/v2 readers remain available; deployed smoke pending |
| D10 | exact first-write rollback floor is recorded before any Production v3/v2 write | ledger + deployment evidence | pending |

## E. Atlas, Library, Explore, Record, content, and SEO gates

| ID | Gate | Required evidence | Status |
|---|---|---|---|
| E1 | public Atlas facets and groups derive from the exact 534 published Record projection, not registry load state; Classic is overlapping and seven structural families cover the published set | data/UI tests | pass: the landing and directory share the checked-in published projection; exact All 534, Classic 94, eight facet counts, seven structural families, and Root Finding 14 are mechanically bound |
| E2 | complete published catalog is discoverable without text search and remains accessible by keyboard/screen reader | component/E2E/a11y | partial: the landing exposes all eight category entry points and the server-rendered directory exposes all 534 canonical destinations with JavaScript disabled; manual screen-reader evidence remains F5 |
| E3 | Library and Formula workspace unify Standard/Mine selection, Source, Edit, Remix, Import, and My Formulas | component/E2E | pending |
| E4 | Lucky selects only eligible complete Records and resolves a pinned Profile | deterministic/eligibility tests | pending |
| E5 | `/formulas/[formulaId]` renders every canonical route; 21 Guide slugs and legacy routes redirect deterministically | route/build tests | pass: the canonical UUID route set and 21 locale-preserving 308 Guide redirects are exact; the Record projection exposes 534 published source/preview/action sets, while unavailable records expose zero runnable CTAs |
| E6 | seven-locale structural key parity; unverified localized names remain absent; fallbacks are explicit | i18n schema tests | partial: Formula Record UI labels have seven-locale structural parity; the published projection identifies exact FractalPark/Fractint/Iterated Dynamics resources with immutable links while the streamlined UI omits Author, Original version, Provenance collection, Rights status, Public scope, Canonical implementation license, and Legacy aliases; the six non-English manual/message projections are model-reviewed against the Commit 19 English authority, hash-bound in the localization review ledger, and human-maintainer approved; the 50 × 7 teaching packages remain pending Commit 20c |
| E7 | 30–50 teaching formulas have reviewed narrative/math/source/controls/related content | content review ledger | pending |
| E8 | selective indexability, canonical/hreflang/JSON-LD/sitemap avoid thin-page inflation | SEO route tests + sampled crawl | partial: Atlas JSON-LD now names the exact 534 published directory instead of the legacy 94-row Explorer catalog, while published Record index/hreflang/sitemap exact-set gates pass; sampled crawl remains pending |
| E9 | IndexNow is not submitted before explicit post-deploy authorization | release checklist | pending |
| E10 | English normative FRM-like v1 reference and author manual document production parser/backend behavior plus disclosed deviations and distinguish published v1 from the Classic Editor | documentation contract tests + technical review | partial: contract-test portion passes; one normative reference and seven author manuals; exact stdlib/guard/limit vocabulary, asset revisions, relative links, directive/namespace/literal/precedence behavior, eight troubleshooting reasons, and the revision-4 fact that 534 Records are published are mechanically bound; seven examples parse, generate CPU/GLSL artifacts, and complete two finite CPU steps; six localized manuals and 39-key projections pass structural/factual parity, repository-resolved DeepSeek and Kimi K3 review evidence, and human maintainer approval; the known nested-magnitude and terminal-high-surrogate non-conformances remain disclosed with writer activation blocked; exact-tree review closure remains F10 |

## F. Performance, quality, rights, and release gates

| ID | Gate | Required evidence | Status |
|---|---|---|---|
| F1 | full tests, focused suites, type/build/lint gates pass on the candidate | command logs | pass: prior Formula Record scope retained 148 files / 1,418 tests, 3 existing skips, focused 24/24, all 513 PNG bytes matching the pinned manifest, byte-identical 8-row rerender sample, build 601/601, and production E2E 11/11; 2026-08-20 Expected Commit 19 adds 149 files passed + 2 skipped files / 1,430 tests + 3 existing skips, documentation/Guide 19/19, lint 0 errors (8 existing warnings), build 601/601, FRM Guide + Formula Atlas E2E 6/6, and 390 px Guide-to-Editor 1/1; the 2026-08-22 Expected Commit 22 correction retains full 156 files passed + 2 skipped files / 1,504 tests + 3 existing skips and rebuilds 804/804 pages in 123.70 s with 1,403,346,944-byte peak RSS and no swap; focused rollback contracts pass 8, 35, 16, and 7 tests, and the final build passes feature-off plus 320/390 px E2E 3/3 |
| F2 | Atlas/Explore initial payload, build memory/time, cold/hot formula select, interaction latency, preview decode, and runtime memory meet frozen baselines | performance report | partial: the deterministic 534-published set and static asset budgets pass; the contract pins facet P75 ≤100 ms, selected/loading max ≤100 ms, hot P95 ≤300 ms, cold desktop/mobile P95 ≤2/3 s, exact 50/50/30/20 samples, isolated-context cold cache, precompiled hot cache, and zero initial/hot `.frm` requests. The current diagnostic envelope records 214 dialog descendants and a 79,624-byte published index while correctly leaving release-qualifying browser/device arrays empty; real-GPU and real-device samples remain required |
| F3 | published-N assets do not ship as one eager JS chunk; sharding/lazy loading is verified in build output | bundle/build audit | pass: the post-build audit finds zero copied `.frm` files and zero published-only definition bodies, and separately freezes the 89-definition overlap already shipped as native recipes |
| F4 | responsive desktop/mobile and reduced-motion behavior pass | screenshots/E2E/manual QA | partial: Formula Record affected-path production E2E previously passed 11/11; the corrected Standard Library layer passes full-width keyboard/focus behavior at both 320 and 390 px on the final build; cross-browser physical-device and reduced-motion evidence remains pending |
| F5 | keyboard, focus, labels, contrast, and screen-reader semantics pass | automated + manual a11y | partial: bounded keyboard controls, focus trap/return, loading live status, busy state, and visible fail-closed alerts pass automated component/E2E checks; manual screen-reader and contrast evidence remains required |
| F6 | Chrome/Safari/Firefox and iPhone/iPad/desktop walking skeletons pass, including GPU recovery | real-device evidence | pending: release verifier now requires desktop Chromium/Firefox real-GPU rows plus iPhone Safari, iPad Safari, Android Chrome, and screen-reader/keyboard evidence; all physical-device rows remain unclaimed |
| F7 | the exact rights/publication ledger is complete; P/A evidence and C separated-independent-rewrite basis are recorded | rights/publication audit | pass: every published Record has a recorded publication basis; unavailable identities expose no runnable CTA |
| F8 | public repository/build/log/route/fixture leakage scan is zero | L1 scan | pass: release-mode publication isolation verifies 534 published Definitions and matching runtime/preview rows, zero source maps, and no private evidence in tracked or built files |
| F9 | backup/restore, writer-off, feature-off, LKG reader/runtime, and alias resolver rollback drills pass | runbook evidence | partial: five source-bound local artifacts now pass writer-off, feature-off, LKG runtime, reader floor, and exact alias resolver checks. Release remains blocked until a SHA/target-bound Preview or Staging artifact passes the exact seven-step backup → disable writer/feature → activate LKG → verify reader/alias → restore-current drill |
| F10 | Draft PR has independent review, all findings resolved or explicitly deferred outside v0.4.19 without weakening scope | review ledger | pending |

## Release interpretation

- A green prototype row is not production activation.
- A private legacy projection or synthetic contract does not create a publication
  decision or runnable implementation.
- Public activation requires an exact rights decision for every frozen identity.
  Runtime, pinned source, default Profile, preview, and Remix exist only for the
  534 published Records.
- Rights-incompatible identities remain unavailable outside the current MIT
  implementation bundle.
- Unclosed work holds the affected row; it does not block unrelated published rows.
- Merge, Production migration, writer enablement, deployment, tag, release, and
  IndexNow remain separate authorization gates.
