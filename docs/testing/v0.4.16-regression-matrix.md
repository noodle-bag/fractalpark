# v0.4.16 Regression Matrix — Cloud-First Navigation & Unified Creation

Companion to ADR 0006 and `web-creation-loop-v1.md` §17. Every row must be
verified against the real stack (local Supabase + app) before release;
focused rows run per commit, the full matrix runs on the release candidate.

## A. Contracts & clean cut

| # | Check | Method |
|---|---|---|
| A1 | No runtime `localStorage` access in `src/` outside `src/test/` (static guard, comments and guard script excluded; the guard script itself lands in the browser-storage-removal commit — rows A1/A2 execute from that commit onward) | `npm run guard:no-local-storage` |
| A2 | Legacy keys `fractalpark-artworks-v1` / `myfrac-saved-fractals` / `myfrac-custom-formulas` unreferenced by runtime code | static guard |
| A3 | Playwright storage probe: zero reads/writes of legacy keys during Explore, Save, My Works, Editor, import journeys | e2e injection |
| A4 | ADR 0001/0005 unchanged; ADR 0006 present and linked from docs index | link check |

## B. Navigation & session

| # | Check | Method |
|---|---|---|
| B1 | Language dropdown preserves path + query + hash across EN/ZH (incl. `?draft=`) | unit + e2e |
| B2 | Navbar 48 px; no horizontal overflow at 390×844 / 768×1024 / 1440×900 | e2e viewport |
| B3 | Five session states render distinctly; `unavailable` never shows Sign in | unit + forced-failure e2e |
| B4 | Sign out in navbar for authenticated; Sign in for anonymous; fixed-width loading skeleton (no layout shift) | e2e |
| B5 | My Works shows no Sign out / Delete account UI; deletion API + safety tests intact | e2e + unit |
| B6 | Footer has no Formula Atlas link; Privacy/Terms/Rules remain | e2e |
| B7 | Keyboard: dropdown, sign in/out, mobile menu fully operable; aria-labels present | a11y e2e |
| B8 | OTP first screen states purpose (cloud save / cross-device / publish required) in both locales | unit |

## C. Explore cloud-authoritative save

| # | Check | Method |
|---|---|---|
| C1 | Anonymous Save → OTP → exactly one cloud draft created (frozen intent envelope) | e2e |
| C2 | Repeat Save increments revision by exactly 1; lost-response retry with same idempotency key does not duplicate | API test |
| C3 | `?draft=<uuid>` loads owner's draft; user B gets the same not_found as a nonexistent id | API negative |
| C4 | Revision conflict never overwrites remote; reload / save-as both work | unit + e2e |
| C5 | Cloud failure (offline/503): no success toast, no localStorage access, download offered | e2e injection |
| C6 | Sign out keeps canvas memory state but strips draft identity; next Save asks OTP again | e2e |
| C7 | SSR first frame for `?draft=` is the deterministic shell; no private data, no default-fractal impersonation | SSR contract test |
| C8 | Open draft from My Works lands on `?draft=` with cloud state (no local hydration) | e2e |

## D. My Works, remix, import

| # | Check | Method |
|---|---|---|
| D1 | My Works renders Cloud Drafts + Published only; second device sees same lists | e2e |
| D2 | Rename via revision-safe PATCH; delete removes draft; publish flow intact | e2e + API |
| D3 | Community remix (anonymous) loads transient Explore state; first Save creates cloud draft with `publication` provenance | e2e |
| D4 | `.fractal.json` import registers formula assets as in-memory only; never silently writes My Formulas | e2e |
| D5 | No duplicate local/cloud entries; no On-this-device section | e2e |

## E. Custom formulas (cloud)

| # | Check | Method |
|---|---|---|
| E1 | owner A/B isolation, anonymous rejected, service-role-only base table | db:test negative |
| E2 | 50-record quota and 64 KiB source budget enforced server-side with stable error codes | db:test |
| E3 | create/update validate name, experience hint, built-in ID conflict, and `compileFrm` server-side (Node-side checks covered by unit tests; DB shape constraints by db:test) | unit + db:test |
| E4 | Revision optimistic concurrency; stale writer gets conflict, no silent overwrite | API test |
| E5 | Idempotent create/update/delete (key + request hash + replay) | API test |
| E6 | List returns summary only; detail fetched on demand | API test |
| E7 | Account deletion removes formulas in the same staged flow | db:test |
| E8 | Second device lists/opens/edits/deletes the same formula | e2e |
| E9 | Deleting a library formula never breaks drafts that snapshot its source | e2e |
| E10 | `.frm` import stays an in-memory editor draft until explicit Save | e2e |

## F. Custom-formula publication

| # | Check | Method |
|---|---|---|
| F1 | Publish accepts a single referenced, hash-matched, ≤64 KiB, server-compilable formula asset | API |
| F2 | Rejects: unreferenced, hash mismatch, oversize, ID conflict, compile failure | API negative |
| F3 | Publish dialog requires explicit source-public + MIT confirmation; server refuses without it | e2e + API |
| F4 | Publication snapshot freezes source/hash, `MIT` license, `formula_source` scope, attestation version | db:test |
| F5 | Community detail shows formula license; source download matches frozen bytes | e2e |
| F6 | Remix of a custom-formula publication reproduces the same rendering | e2e |
| F7 | Withdraw/deletion stops new reads/downloads but preserves minimal provenance; licenses not revoked retroactively | API |
| F8 | Built-in-only publications keep null/not-applicable formula license fields; no backfill | db:test |

## G. Public facts & release

| # | Check | Method |
|---|---|---|
| G1 | README/About/llms/messages contain no local-first / device-only claims for current abilities | readme:check + copy guard |
| G2 | Terms documents formula-source MIT publication no later than the commit that ships custom-formula publishing | docs check |
| G3 | Version, CHANGELOG, release notes; 0.4.x numbering continuous | release check |
| G4 | Forward-only migration replays on staging; preflight parity; production backup first | runbook |
| G5 | Release-day re-verification of the no-external-user-data premise recorded in the ledger | process |
| G6 | Rollback-compatibility note: v0.4.15 app reading a v0.4.16 draft envelope — documented behavior | docs |
| G7 | IndexNow only for affected public canonical URLs; never My Works / draft / noindex URLs | process |
